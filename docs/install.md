# Install

```bash
uv tool install ableton-ctrl
ableton-ctrl install
```

Use `--with-pi` to install Pi artifacts and `--with-kb --kb-path PATH` to create a user-owned knowledge base. `--home PATH` is for isolated tests.
