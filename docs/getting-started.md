# Getting started

Install `ableton-ctrl`, run `ableton-ctrl install`, then select `AbletonCtrl` as a Live Control Surface. Run `ableton-ctrl doctor` to check the local bridge and Live connection. The tool only inspects Live; it does not modify a Set.
