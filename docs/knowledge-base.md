# Knowledge base

The CLI performs deterministic work: initialization, import, hashing, manifest updates, and structural linting. Pi performs semantic summaries and synthesis. Set `ABLETON_CTRL_KB_PATH` before `kb add`, `kb status`, or `kb lint`.
